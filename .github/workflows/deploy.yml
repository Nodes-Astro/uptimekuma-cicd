name: Deploy (Production)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.PROD_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H "${{ secrets.PROD_HOST }}" >> ~/.ssh/known_hosts

      - name: Upload compose files
        run: |
          scp -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no uptimekuma-cicd/docker-compose.yml ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.REMOTE_DIR }}/docker-compose.yml
          scp -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no uptimekuma-cicd/Caddyfile          ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.REMOTE_DIR }}/Caddyfile


      - name: Remote deploy (docker compose)
        run: |
          ssh -i ~/.ssh/id_rsa ${{ secrets.PROD_USER }}@${{ secrets.PROD_HOST }} << 'EOF'
            set -e
            cd "${{ secrets.PROD_PATH }}"
            docker compose pull
            docker compose up -d
            docker image prune -f
          EOF
